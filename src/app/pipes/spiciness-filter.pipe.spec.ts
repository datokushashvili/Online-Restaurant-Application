import { SpicinessFilterPipe } from './spiciness-filter.pipe';

describe('SpicinessFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SpicinessFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
