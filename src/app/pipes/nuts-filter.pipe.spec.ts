import { NutsFilterPipe } from './nuts-filter.pipe';

describe('NutsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new NutsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
